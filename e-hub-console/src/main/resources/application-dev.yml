spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    platform: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://82.156.97.190:3306/e_hub?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ehub666$

  # redis
  redis:
    host: 82.156.97.190
    port: 6379
    database: 0
    password: ehub666$
    timeout: 6000
    jedis:
      pool:
        max-active: 300
        min-idle: 10
        max-idle: 100
        max-wait: 4000

# 本地开发认证配置。生产环境仍必须通过 CONSOLE_AUTH_TOKEN_SECRET 显式提供密钥。
console:
  auth:
    token-secret: ${CONSOLE_AUTH_TOKEN_SECRET:e-hub-console-dev-only-token-secret}
    session-store: ${CONSOLE_AUTH_SESSION_STORE:local}

#大数据
bigdata:
  userKey: dev-key
  data-collector: http://localhost:8080

#监控
management:
  endpoints:
    web:
      exposure:
        include: '*'

#appId
app:
  id: e-hub-console-dev

swagger:
  enable: true

# apollo配置
apollo:
  check: default

# 服务器配置
server:
  keystore-alias: default-alias
