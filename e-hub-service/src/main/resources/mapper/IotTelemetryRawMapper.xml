<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.sl.ehub.service.mapper.IotTelemetryRawMapper">

  <insert id="insertRaw" parameterType="cn.sl.ehub.service.vo.IotTelemetryRaw">
    INSERT INTO iot_telemetry_raw (
      interface_type, source_code, ent_id, project_id,
      device_id, device_code, point_code, external_device_id, external_metric,
      data_time, raw_value, receive_time, raw_payload, match_status, match_reason, create_time
    ) VALUES (
      #{interfaceType}, #{sourceCode}, #{entId}, #{projectId},
      #{deviceId}, #{deviceCode}, #{pointCode}, #{externalDeviceId}, #{externalMetric},
      #{dataTime}, #{rawValue}, #{receiveTime}, #{rawPayload}, #{matchStatus}, #{matchReason}, #{createTime}
    )
  </insert>

</mapper>
