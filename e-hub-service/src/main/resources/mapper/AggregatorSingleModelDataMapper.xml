<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.sl.ehub.service.mapper.AggregatorSingleModelDataMapper">

  <sql id="modelColumns">
    id,
    aggregator_id AS aggregatorId,
    ent_id AS entId,
    resource_type_id AS resourceTypeId,
    energy_station AS energyStation,
    energy_station_code AS energyStationCode,
    power_cap AS powerCap,
    area,
    user_type AS userType,
    device_manufacture AS deviceManufacture,
    save_heat AS saveHeat,
    owner,
    controll
  </sql>

  <select id="selectModelList" resultType="cn.sl.ehub.service.vo.AggregatorSingleModelData">
    SELECT
    <include refid="modelColumns"/>
    FROM aggregator_single_model_data
    <where>
      <if test="aggregatorId != null and aggregatorId != ''">
        AND aggregator_id = #{aggregatorId}
      </if>
      <if test="entId != null and entId != ''">
        AND ent_id = #{entId}
      </if>
      <if test="resourceTypeId != null and resourceTypeId != ''">
        AND resource_type_id = #{resourceTypeId}
      </if>
      <if test="energyStationCode != null and energyStationCode != ''">
        AND energy_station_code = #{energyStationCode}
      </if>
      <if test="energyStation != null and energyStation != ''">
        AND energy_station LIKE CONCAT('%', #{energyStation}, '%')
      </if>
      <if test="energyStationCodes != null">
        <choose>
          <when test="energyStationCodes.size() > 0">
            AND energy_station_code IN
            <foreach collection="energyStationCodes" item="code" open="(" separator="," close=")">
              #{code}
            </foreach>
          </when>
          <otherwise>
            AND 1 = 0
          </otherwise>
        </choose>
      </if>
    </where>
    ORDER BY id DESC
  </select>

  <select id="selectModelById" resultType="cn.sl.ehub.service.vo.AggregatorSingleModelData">
    SELECT
    <include refid="modelColumns"/>
    FROM aggregator_single_model_data
    WHERE id = #{id}
  </select>

  <select id="countByEnergyStationCode" resultType="java.lang.Integer">
    SELECT COUNT(1)
    FROM aggregator_single_model_data
    WHERE energy_station_code = #{energyStationCode}
    <if test="excludeId != null">
      AND id != #{excludeId}
    </if>
  </select>

</mapper>
