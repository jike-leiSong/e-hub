<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.sl.ehub.service.mapper.ConsoleRolePermissionMapper">

    <resultMap id="BaseResultMap" type="cn.sl.ehub.service.vo.ConsoleRolePermission">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="role_id" jdbcType="VARCHAR" property="roleId"/>
        <result column="permission_code" jdbcType="VARCHAR" property="permissionCode"/>
        <result column="create_time" jdbcType="VARCHAR" property="createTime"/>
        <result column="update_time" jdbcType="VARCHAR" property="updateTime"/>
    </resultMap>

    <select id="listByRoleIds" resultMap="BaseResultMap">
        SELECT id,
               role_id,
               permission_code,
               create_time,
               update_time
        FROM console_role_permission
        WHERE role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </select>

    <select id="listByRoleId" resultMap="BaseResultMap">
        SELECT id,
               role_id,
               permission_code,
               create_time,
               update_time
        FROM console_role_permission
        WHERE role_id = #{roleId}
    </select>

    <delete id="deleteByRoleId">
        DELETE FROM console_role_permission
        WHERE role_id = #{roleId}
    </delete>
</mapper>
