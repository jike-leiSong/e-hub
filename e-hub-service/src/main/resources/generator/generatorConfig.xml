<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
        PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<generatorConfiguration>
    <context id="Mysql" targetRuntime="MyBatis3Simple" defaultModelType="flat">

        <!--通用 Mapper 插件，可以生成带注解的实体类-->
        <plugin type="tk.mybatis.mapper.generator.MapperPlugin">
            <property name="mappers" value="tk.mybatis.mapper.common.Mapper"/>
            <property name="caseSensitive" value="true"/>
            <property name="forceAnnotation" value="true"/>
        </plugin>

        <!--关闭自动生成的注释-->
        <commentGenerator>
            <property name="suppressDate" value="true"/>
            <property name="suppressAllComments" value="true"/>
        </commentGenerator>

        <jdbcConnection driverClass="com.mysql.cj.jdbc.Driver"
                        connectionURL="jdbc:mysql://10.39.30.21:3306/load-aggregator?characterEncoding=utf8&amp;serverTimezone=Asia/Shanghai"
                        userId="fn_db_test" password="fn_db_test20180411">
        </jdbcConnection>

        <!--MyBatis 生成器只需要生成 Model-->
        <javaModelGenerator targetPackage="cn.sl.ehub.upstream.vo"
                            targetProject="src\main\java"/>

        <!-- 生成的SQLMapper映射文件包名和位置 -->
        <sqlMapGenerator targetPackage="mapper"
                         targetProject="src\main\resources">
        </sqlMapGenerator>

        <!--MyBatis 生成器只需要生成 Mapper-->
        <javaClientGenerator targetPackage="cn.sl.ehub.upstream.mapper"
                             targetProject="src\main\java"
                             type="XMLMAPPER"/>

    </context>
</generatorConfiguration>
